# Another driver binds the same PCI ids, and whichever loads first owns
# the card. Keep it from autoloading so that ajv4l2 gets the card at boot.
# Remove this file (or the package) to go back.
blacklist ajantv2
